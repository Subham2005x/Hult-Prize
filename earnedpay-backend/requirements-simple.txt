# Alternative Requirements for Python 3.13 on Windows
# This version avoids packages that require Rust compilation

# Core FastAPI (older version with pre-built wheels)
fastapi==0.103.2
uvicorn==0.23.2

# Firebase (older stable version)
firebase-admin==6.0.1

# Validation (without Pydantic - using dataclasses instead)
# We'll use Python's built-in dataclasses for now
python-multipart==0.0.6
python-dotenv==1.0.0
httpx==0.24.1
python-dateutil==2.8.2

# Optional: If you want to try Pydantic later
# Uncomment these lines and try installing:
# pydantic==2.4.2
# pydantic-settings==2.0.3
